# =============================================================================
# APP OVERLAY - BASE
# =============================================================================
# Shared configuration for all environments.
# =============================================================================

services:
  [[ .service_name ]]:
[[ if .watchtower ]]
    labels:
      - "com.centurylinklabs.watchtower.enable=true"
[[ end ]]
    env_file:
      - app/overlays/base/.env
